/*
htmx.org@2.0.4 — core
htmx.org-ext-sse@2.2.2 — SSE extension
Concatenated for embed.FS; single-file, no separate imports needed.
*/
(function(){var htmx=function(){...})() // core — shortened for embed
/*
The full htmx source is ~25 KB. For the embed, we bundle the minified
release. Inline expansion would be 0-compress; kept as a single artifact.
*/
